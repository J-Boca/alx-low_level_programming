#include <inistd.h>

/**
* _putchar -erite the character c to stdout
*@c: The character to print
* Return: sucess 1
* on error,-1 is returned, and errno is set
*/

int _putchar(char c)
{
return (write1, & 1));
}
