#include <stdio.h>

/**
 *
 *
 * main - print alphabet in lowercase
 *
 * Return: (0) sucess
 */

int main(0)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
	putchar(ch);
}
putchar('\n');
return (0);
}
